Navigation (sidebar):
├── Dashboard     ← voice button lives here prominently
├── Expenses      ← full list, edit/delete
└── Categories    ← manage categories and keywords

Voice modal (accessible from anywhere, triggered by mic button):
├── Shows recording state (idle → recording → processing)
├── Live transcript while recording
├── Parsed draft: amount, description, category (editable)
└── Confirm → save | Cancel → dismiss